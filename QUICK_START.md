# 🚀 Windows + Visual Studio 2022 馬力歐遊戲開發快速開始

> 從零開始到執行遊戲，只需要跟著這 6 個步驟！
>
> 預估完成時間：1-2 小時

---

## 📥 第一步：下載所需軟件

### 1. Visual Studio 2022 Community（必須）

**下載網址**：https://visualstudio.microsoft.com/zh-hant/downloads/

- 檔案大小：約 3-4 GB
- 安裝時選擇：**「使用 C++ 的桌面開發」** 工作負載
- 安裝時間：約 30-40 分鐘

### 2. EGE 圖形庫（必須）

**下載網址**：https://github.com/wysaid/xege/releases

- 下載最新的 `ege-xxxx.zip`
- 解壓到 `C:\ege\`（記住這個路徑）

### 3. 馬力歐遊戲原始碼（必須）

**方法 A**：直接下載 ZIP
1. 前往：https://github.com/ByteTuxiaobei/Mario
2. 點擊綠色 **Code** 按鈕 → **Download ZIP**
3. 解壓到 `C:\Projects\Mario\`

**方法 B**：使用 Git（需先安裝 Git）
```bash
git clone https://github.com/ByteTuxiaobei/Mario.git
```

---

## ⚙️ 第二步：安裝 Visual Studio 2022

1. 執行下載的安裝程式
2. 在「工作負載」頁面，勾選 **「使用 C++ 的桌面開發」**
3. 點擊右下角「安裝」按鈕
4. 等待安裝完成（約 30 分鐘）
5. 安裝完成後，重新啟動電腦

---

## 🎨 第三步：配置 EGE 圖形庫

### 情況 A：如果專案已有 .sln 檔案

1. 打開 Visual Studio 2022
2. 開啟 `Mario.sln` 專案檔
3. 右鍵點擊專案名稱 → **屬性**
4. 按照下面的設置修改

### 情況 B：如果需要建立新專案

1. 打開 Visual Studio 2022
2. 選擇「建立新專案」
3. 搜尋「空白專案」，選擇 **C++** 的空白專案
4. 專案名稱：`MarioGame`
5. 將所有 `.cpp` 和 `.h` 檔案加入專案

### 必須的專案設置

#### 設置 1：Include 目錄

1. 專案屬性 → **C/C++** → **一般**
2. **其他 Include 目錄** 加入：`C:\ege\include`

#### 設置 2：庫目錄

1. 專案屬性 → **連結器** → **一般**
2. **其他程式庫目錄** 加入：`C:\ege\lib`（根據你的系統選擇 x86 或 x64）

#### 設置 3：依賴庫

1. 專案屬性 → **連結器** → **輸入**
2. **其他相依性** 加入：`graphics.lib`

#### 設置 4：字元集

1. 專案屬性 → **進階**
2. **字元集** 選擇：**使用 Unicode 字元集** 或 **使用多位元組字元集**

點擊「確定」儲存設置。

---

## 📁 第四步：準備資源檔案

確保專案目錄結構如下：

```
MarioGame/
├── Mario.sln
├── Mario.vcxproj
├── src/
│   ├── main.cpp
│   ├── mario.cpp
│   └── ...（所有 .cpp 和 .h 檔案）
├── resources/          ← 重要！
│   ├── images/
│   ├── sounds/
│   └── graphics.txt
└── level_data/         ← 重要！
    └── *.mio
```

**重要提醒**：
- 確認 `resources/` 和 `level_data/` 資料夾存在
- 將這兩個資料夾複製到編譯輸出目錄（`Debug/` 或 `Release/`）

---

## 🔧 第五步：編譯專案

1. 在 Visual Studio 上方工具列：
   - **組態**：選擇 `Debug`
   - **平台**：選擇 `x64`（如果你的系統是 64 位元）

2. 點擊 **建置** → **建置方案**（或按 `Ctrl+Shift+B`）

3. 查看「輸出」視窗：
   - ✅ 顯示「建置成功」→ 繼續下一步
   - ❌ 有錯誤訊息 → 查看「錯誤清單」並修正

### 常見編譯錯誤：

**錯誤 1**：`找不到 graphics.h`
- **解決**：檢查 Include 目錄設置是否正確

**錯誤 2**：`無法解析的外部符號`
- **解決**：檢查是否正確添加 `graphics.lib`

**錯誤 3**：`字元集錯誤`
- **解決**：將字元集改為「使用多位元組字元集」

---

## ▶️ 第六步：執行遊戲

1. 確認編譯成功（沒有錯誤）

2. **複製資源檔案**（重要！）：
   - 將 `resources/` 資料夾複製到 `Debug/` 或 `Release/` 目錄
   - 將 `level_data/` 資料夾也複製過去

3. 按 `F5` 開始執行（帶偵錯）
   - 或按 `Ctrl+F5` 執行但不偵錯

4. 遊戲視窗應該會打開！🎮

---

## 🎮 如何遊玩

### 基本操作

- **← →** 左右移動
- **Space** 跳躍
- **Shift** 衝刺奔跑
- **F** 發射火球（需要火焰花道具）
- **P** 暫停遊戲
- **ESC** 返回選單

### 遊戲目標

1. 收集金幣增加分數
2. 吃蘑菇變大，吃火焰花獲得火球能力
3. 踩扁敵人或用火球攻擊
4. 到達終點旗杆過關

---

## 🌏 第七步：繁體中文化（可選）

如果遊戲介面是英文或簡體中文，你想改成繁體中文：

請參考：**[遊戲繁體中文化指南](./CHINESE_LOCALIZATION.md)**

主要步驟：
1. 使用 `Ctrl+Shift+F` 搜尋遊戲中的英文字串
2. 改為繁體中文（例如：`"Game Over"` → `"遊戲結束"`）
3. 設置檔案編碼為 UTF-8
4. 重新編譯並測試

---

## 📚 詳細文檔

- **[Visual Studio 2022 完整安裝指南](./SETUP_GUIDE_VS2022.md)**
  詳細的環境配置、常見問題排除

- **[遊戲繁體中文化指南](./CHINESE_LOCALIZATION.md)**
  如何將所有介面文字改為繁體中文

- **[馬力歐遊戲分析報告](./MARIO_GAME_ANALYSIS.md)**
  了解遊戲架構、已實現功能、學習資源

---

## ✅ 完成檢查清單

- [ ] Visual Studio 2022 已安裝（包含 C++ 工作負載）
- [ ] EGE 圖形庫已下載並解壓到 `C:\ege\`
- [ ] 馬力歐遊戲原始碼已下載
- [ ] 專案在 Visual Studio 中成功開啟
- [ ] Include 和 Lib 目錄已正確設置
- [ ] 專案編譯成功（沒有錯誤）
- [ ] 資源檔案（resources/, level_data/）已複製到輸出目錄
- [ ] 遊戲可以執行並正常顯示

---

## 🆘 遇到問題？

### 方案 1：查看詳細指南
- [Visual Studio 2022 安裝指南](./SETUP_GUIDE_VS2022.md) 的「常見問題排除」章節

### 方案 2：檢查錯誤訊息
1. 在 Visual Studio 查看「錯誤清單」視窗
2. 記下錯誤代碼（例如：C1083, LNK2019）
3. 根據錯誤類型對照本指南的「常見錯誤」章節

### 方案 3：確認基本設置
- EGE 路徑是否正確
- 專案屬性中的 Include 和 Lib 路徑是否正確
- 字元集設置是否正確
- 資源檔案是否在正確位置

---

## 🎯 成功執行後，你可以：

1. **遊玩完整遊戲** - 體驗經典馬力歐
2. **研究程式碼** - 學習遊戲開發技巧
3. **修改遊戲內容** - 加入自己的創意
4. **繁體中文化** - 讓介面更友善
5. **製作自訂關卡** - 使用 .mio 關卡編輯器

---

## 📖 推薦學習順序

1. ✅ **先執行遊戲** - 確保環境正確
2. 📖 **閱讀分析報告** - 了解遊戲架構
3. 🔍 **研究原始碼** - 學習實現方式
4. 🌏 **繁體中文化** - 修改介面文字
5. ✨ **自訂修改** - 加入新功能
6. 🎮 **製作關卡** - 設計自己的關卡

---

祝你開發順利，玩得開心！🎮🍄⭐

如果有任何問題，請查看詳細文檔或搜尋相關錯誤訊息。
